# Extra dependencies for the double-click desktop wrapper (see desktop/).
pywebview>=5.0
# Shared desktop-wrapper runtime (native-window launcher + same-origin ASGI app),
# resolved from the sibling thehub-pr checkout as an editable local path so the
# code is edited once for the whole federation (frozen builds bundle it).
-e ../thehub-pr/packages/prii_desktop
