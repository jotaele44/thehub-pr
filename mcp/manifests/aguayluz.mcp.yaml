# MCP manifest — AguaYLuz (water/energy/utilities)
# Declares capabilities only; TheHub's capability registry and validators
# are the source of truth for what each capability means and permits.
project: aguayluz

inherits:
  - federation-core
  - github-bridge
  - provenance

capabilities:
  - terrain
  - weather
  - utilities
  - osha

write_policy:
  default: read_only
  allowed_writes: []
